# 웹에 공개되면 안 되는 개인/비공개 자료 (로컬에만 보관)
*.docx
*.pdf
*.hwp
PLAN.md

# 단, files/ 안의 PDF는 공개용(가입 선물 등)이므로 추적 허용
!files/*.pdf

# Netlify Drop용 임시 복사 폴더 (방법 B에서는 루트 파일이 원본)
site/

# OneDrive / OS 부산물
desktop.ini
Thumbs.db
.DS_Store
tools/_digests*.txt
